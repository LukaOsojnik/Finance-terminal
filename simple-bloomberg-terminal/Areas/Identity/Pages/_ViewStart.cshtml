@{
    // Render all Identity pages (Login, Register, our ExternalLogin override, etc.) inside the main
    // site layout so they get Bootstrap + the navbar + site chrome instead of the bare Identity.UI default.
    Layout = "/Views/Shared/_Layout.cshtml";
}
